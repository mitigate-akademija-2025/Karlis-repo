<% content_for :title, "Editing quiz" %>

<div class="flex flex-col items-center w-full">
  <h1 class="text-3xl font-bold text-blue-700 mb-8 text-center">Editing quiz</h1>

  <div class="w-full max-w-xl">
    <%= render "form", quiz: @quiz %>
  </div>

  <div class="mt-8 flex gap-4">
    <%= link_to "Show this quiz", @quiz, class: "text-white bg-blue-600 px-4 py-2 rounded" %>
    <%= link_to "Back to quizzes", quizzes_path, class: "text-white bg-green-600 px-4 py-2 rounded" %>
  </div>
</div>
