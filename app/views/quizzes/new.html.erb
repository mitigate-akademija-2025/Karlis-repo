<% content_for :title, "New quiz" %>

<div class="flex flex-col items-center w-full">
  <h1 class="text-3xl font-bold text-blue-700 mb-8 text-center">New quiz</h1>

  <div class="w-full max-w-xl">
    <%= render "form", quiz: @quiz %>
  </div>

  <div class="mt-8">
    <%= link_to "Back to quizzes", quizzes_path, class: "text-white bg-green-600 px-4 py-2 rounded" %>
  </div>
</div>
